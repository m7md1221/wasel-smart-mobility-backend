# Server
PORT=4000

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=wasel_palestine

# Auth
JWT_SECRET=CHANGE_ME_TO_A_LONG_RANDOM_STRING

# Routing Provider (OpenRouteService by default)
ROUTING_PROVIDER_URL=https://api.openrouteservice.org
ROUTING_TIMEOUT_MS=5000
ROUTING_CACHE_TTL_SECONDS=120
ORS_API_KEY=CHANGE_ME

# Notifications (optional)
EMAIL=example@gmail.com
PASSWORD=app-password-or-smtp-password

# Telegram bot (optional)
TELEGRAM_BOT_TOKEN=CHANGE_ME
